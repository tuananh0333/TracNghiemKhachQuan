<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tracnghiemkhachquan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.ResultActivity" android:screenOrientation="portrait" android:label="@string/ketqua"></activity>
        <activity android:name=".activities.fifthActivity" android:screenOrientation="landscape" android:label="@string/a5"/>
        <activity android:name=".activities.fourthActivity" android:screenOrientation="landscape" android:label="@string/a4"/>
        <activity android:name=".activities.thirdActivity" android:screenOrientation="landscape" android:label="@string/a3"/>
        <activity android:name=".activities.secondActivity" android:screenOrientation="landscape" android:label="@string/a2"/>
        <activity android:name=".activities.firstActivity" android:screenOrientation="landscape" android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>